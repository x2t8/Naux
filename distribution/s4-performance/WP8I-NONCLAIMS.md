# S4-WP8I non-claims

WP8I admits only a clock-free controlled-host protocol for the isolated
register-residency candidate.

It does not retain a host observation, execute a benchmark, collect a timing
sample, replace the WP5F baseline, admit a measurement runner, or support a
performance claim. An eligible observation remains ephemeral stdout until a
later authority explicitly defines retention and acquisition.
