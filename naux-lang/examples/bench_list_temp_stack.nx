~ rite
    $n = 300000
    $i = 0
    ~ while $i < $n
        $__ = len([$i, $i + 1, $i + 2])
        $__ = len([$i, $i + 3, $i + 4])
        $t = $i + 5
        $__ = len([$t, $t + 1, $t + 2])
        $i = $i + 1
    ~ end

    ^ $n
~ end
